export enum Modus{
    Search = 1,
    CreateTicket = 2    
}

export enum StatusType{
    success = "success",
    info = "info",
    warning = "warning",
    error = "error"
}

export enum HTTPStatusCode{
    ok = 200,
    bad_request = 400,
    unauthorized = 401,
    forbidden = 403,
    server_error = 500
}